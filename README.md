Find squares with maximal digit average
=======================================

Given a positive integer:math:`n` find the :math:`n` digit decimal
number which is a perfect square with maximal digit sum.
